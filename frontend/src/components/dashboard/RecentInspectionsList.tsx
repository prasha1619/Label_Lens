import React from 'react';
import { ArrowRight, Package } from 'lucide-react';
import { InspectionListItem } from '../../types/inspection';
interface Props { inspections: InspectionListItem[]; onViewAll: () => void; onSelectInspection?: (id: string) => void; }
const displayStatus = (status: string) => status.replace(/_/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
export const RecentInspectionsList: React.FC<Props> = ({ inspections, onViewAll, onSelectInspection }) => <div className="rounded-2xl bg-[#0d1430] border border-[#1d274d] p-5 shadow-xl flex flex-col h-full"><div className="flex items-center justify-between mb-3"><h3 className="text-sm font-bold text-white tracking-wide">Recent Inspections</h3><button onClick={onViewAll} className="text-xs font-semibold text-purple-400 hover:text-purple-300">View All</button></div><div className="space-y-2 flex-1">{inspections.length ? inspections.slice(0, 5).map(item => { const good = item.overall_status === 'COMPLIANT'; return <button key={item.id} onClick={() => onSelectInspection?.(item.id)} className="w-full flex items-center justify-between gap-2 p-2 rounded-xl text-left hover:bg-[#141d3d] border border-transparent hover:border-[#222e5a]"><div className="flex min-w-0 items-center gap-3"><div className="w-9 h-9 rounded-lg bg-[#182348] border border-[#263566] flex items-center justify-center text-blue-300 shrink-0"><Package className="w-4 h-4" /></div><div className="min-w-0"><p className="text-xs font-bold text-slate-100 truncate">{item.product_name || item.original_filename || 'Unnamed product'}</p><p className="text-[11px] text-slate-400 truncate">{item.product_category.replace(/_/g, ' ')}</p></div></div><div className="shrink-0 text-right"><span className={`px-2 py-0.5 rounded-md text-[9px] font-semibold border ${good ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/20' : 'bg-rose-500/10 text-rose-400 border-rose-500/20'}`}>{displayStatus(item.overall_status)}</span><p className="mt-1 text-[9px] text-slate-500">{new Date(item.created_at).toLocaleDateString()}</p></div></button>; }) : <div className="flex h-full min-h-40 items-center justify-center text-center text-xs text-slate-500">Saved product inspections will appear here.</div>}</div><div className="pt-3 border-t border-[#182348] text-center mt-2"><button onClick={onViewAll} className="inline-flex items-center gap-1.5 text-xs font-semibold text-purple-400 hover:text-purple-300">View All Inspections <ArrowRight className="w-3.5 h-3.5" /></button></div></div>;
