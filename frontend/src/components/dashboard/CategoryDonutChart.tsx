import React from 'react';
interface Props { distribution: Record<string, number>; total: number; }
const colors = ['#8b5cf6', '#3b82f6', '#10b981', '#f59e0b', '#ec4899', '#64748b'];
const nice = (value: string) => value.replace(/_/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
export const CategoryDonutChart: React.FC<Props> = ({ distribution, total }) => { const entries = Object.entries(distribution); let offset = 0; return <div className="rounded-2xl bg-[#0d1430] border border-[#1d274d] p-5 shadow-xl flex flex-col h-full"><h3 className="text-sm font-bold text-white tracking-wide">Compliance by Category</h3><div className="flex flex-1 flex-col sm:flex-row items-center justify-between gap-4 py-4"><div className="relative w-36 h-36 shrink-0"><svg className="w-full h-full -rotate-90" viewBox="0 0 42 42"><circle cx="21" cy="21" r="15.915" fill="transparent" stroke="#131b3a" strokeWidth="5" />{entries.map(([category, count], index) => { const pct = total ? count / total * 100 : 0; const circle = <circle key={category} cx="21" cy="21" r="15.915" fill="transparent" stroke={colors[index % colors.length]} strokeWidth="5" strokeDasharray={`${pct} ${100-pct}`} strokeDashoffset={-offset} />; offset += pct; return circle; })}</svg><div className="absolute inset-0 flex flex-col items-center justify-center"><span className="text-lg font-extrabold text-white">{total}</span><span className="text-[10px] text-slate-400">Reports</span></div></div><div className="w-full space-y-2 text-xs">{entries.length ? entries.map(([category, count], index) => <div key={category} className="flex items-center justify-between gap-2"><span className="flex items-center gap-2 text-slate-300 truncate"><i className="h-2.5 w-2.5 rounded-full" style={{backgroundColor: colors[index % colors.length]}} />{nice(category)}</span><b className="text-slate-100">{count}</b></div>) : <p className="text-slate-500">No report categories yet.</p>}</div></div></div>; };
